/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No necesitas NADA MÁS.
  // Next.js maneja .env.local y NEXT_PUBLIC_ automáticamente.
};

export default nextConfig;